timeline.overwrite = true
dag.overwrite = true
report.overwrite = true
nextflow.preview.output = true

executor {
    name = 'slurm'
    queue = 'efi'
    queueSize = 128
}

process {
    executor = 'local'
    cpus = 1
    memory = 8.GB
}
