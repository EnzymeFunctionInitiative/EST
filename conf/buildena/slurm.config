timeline.overwrite = true
dag.overwrite = true
report.overwrite = true

executor {
    name = 'slurm'
    queue = 'efi'
    queueSize = 30
}

process {
    executor = 'slurm'
    queue = 'efi'
    cpus = 1
    memory = 50.GB

    withName: unzip_files {
        memory = 4.GB
    }
}
